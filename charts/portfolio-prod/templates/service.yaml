apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio-prod.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "portfolio-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "portfolio-prod.selectorLabels" . | nindent 4 }}
