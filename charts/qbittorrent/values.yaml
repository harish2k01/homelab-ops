podSecurityContext:
  fsGroup: 3000

env:
  PUID: "3000"
  PGID: "3000"
  TZ: Asia/Kolkata
  WEBUI_PORT: "8080"
  TORRENTING_PORT: "6881"

persistence:
  config:
    enabled: true
    size: 128Mi
    storageClassName: nfs-csi

media:
  enabled: true
  existingClaim: media-vault-pvc
  mountPath: /vault
  readOnly: false

httpRoute:
  enabled: true
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
      sectionName: websecure
  hostnames:
    - qbittorrent.k8s.harish2k01.xyz

resources: {}

