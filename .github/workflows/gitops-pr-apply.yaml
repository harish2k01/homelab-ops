name: GitOps PR Apply

on:
  pull_request_target:
    types:
      - closed
    paths:
      - "argocd-apps/**"

concurrency:
  group: gitops-pr-apply-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  request:
    if: >-
      github.event.pull_request.merged == true &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: homelab-ops-runner
    outputs:
      base_sha: ${{ steps.pr.outputs.base_sha }}
      merge_sha: ${{ steps.pr.outputs.merge_sha }}
      number: ${{ steps.pr.outputs.number }}
      app_count: ${{ steps.comment.outputs.app_count }}

    steps:
      - name: Validate merged deployment request
        id: pr
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const pull_number = context.issue.number;
            const { data: pull } = await github.rest.pulls.get({
              owner,
              repo,
              pull_number,
            });

            if (pull.state !== 'closed' || !pull.merged || !pull.merge_commit_sha) {
              core.setFailed(`PR #${pull_number} was not merged`);
              return;
            }
            if (pull.head.repo.full_name !== `${owner}/${repo}`) {
              core.setFailed('Deployments from forked pull requests are not allowed');
              return;
            }

            core.setOutput('base_sha', pull.base.sha);
            core.setOutput('merge_sha', pull.merge_commit_sha);
            core.setOutput('number', String(pull.number));

      - name: Checkout trusted workflow code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ steps.pr.outputs.merge_sha }}
          path: workflow

      - name: Checkout merged revision
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ steps.pr.outputs.merge_sha }}
          path: ops

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: python -m pip install --disable-pip-version-check pyyaml

      - name: Prepare deployment request
        env:
          GITHUB_BASE_SHA: ${{ steps.pr.outputs.base_sha }}
          GITHUB_HEAD_SHA: ${{ steps.pr.outputs.merge_sha }}
          GITHUB_PR_NUMBER: ${{ steps.pr.outputs.number }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        working-directory: ops
        run: python ../workflow/.github/scripts/gitops-pr-apply.py --dry-run

      - name: Create friday-pa token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.FRIDAY_PA_APP_ID }}
          private-key: ${{ secrets.FRIDAY_PA_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}

      - name: Post deployment approval request
        id: comment
        uses: actions/github-script@v7
        env:
          MERGE_SHA: ${{ steps.pr.outputs.merge_sha }}
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const fs = require('fs');
            const marker = '<!-- friday-pa:argocd-apply -->';
            const metadata = JSON.parse(
              fs.readFileSync('ops/gitops-pr-apply-metadata.json', 'utf8'),
            );
            const escapeCell = (value) =>
              String(value).replaceAll('|', '\\|').replaceAll('\n', ' ');
            const status = metadata.applications.length
              ? 'Merged; waiting for `olympus` approval'
              : 'Merged; no Application deployment required';
            const lines = [
              marker,
              '## Argo CD Deployment',
              '',
              `**Status:** ${status}`,
              `**Merge revision:** \`${process.env.MERGE_SHA.slice(0, 12)}\``,
              '',
              '| Application | Namespace | Merged changes |',
              '|---|---|---|',
            ];

            for (const app of metadata.applications) {
              lines.push(
                `| \`${escapeCell(app.name)}\` | \`${escapeCell(app.namespace)}\` | ` +
                `${app.reasons.map(escapeCell).join('<br>')} |`,
              );
            }
            if (!metadata.applications.length) {
              lines.push('| - | - | No deployable Application remains in the merge commit |');
            }
            if (metadata.deferredDeletions.length) {
              lines.push(
                '',
                `Application deletions require manual cleanup: ` +
                metadata.deferredDeletions.map((name) => `\`${name}\``).join(', '),
              );
            }
            if (metadata.applications.length) {
              lines.push(
                '',
                '> The PR is merged. Approve the `olympus` environment to apply the merged ' +
                'Application definitions and sync them, or reject the deployment to skip it.',
              );
            }

            const body = `${lines.join('\n')}\n`;
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number,
              per_page: 100,
            });
            const existing = comments.find(
              (comment) => comment.body && comment.body.includes(marker),
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body,
              });
            }
            core.setOutput('app_count', String(metadata.applications.length));

  deploy:
    needs: request
    if: needs.request.outputs.app_count != '0'
    runs-on: homelab-ops-runner
    environment:
      name: olympus
    outputs:
      started: ${{ steps.started.outputs.value }}

    steps:
      - name: Record approval
        id: started
        run: echo "value=true" >> "$GITHUB_OUTPUT"

      - name: Revalidate approved PR revision
        id: pr
        uses: actions/github-script@v7
        env:
          APPROVED_MERGE_SHA: ${{ needs.request.outputs.merge_sha }}
        with:
          script: |
            const { owner, repo } = context.repo;
            const pull_number = context.issue.number;
            const { data: pull } = await github.rest.pulls.get({
              owner,
              repo,
              pull_number,
            });

            if (pull.state !== 'closed' || !pull.merged || !pull.merge_commit_sha) {
              core.setFailed(`PR #${pull_number} is not merged`);
              return;
            }
            if (pull.merge_commit_sha !== process.env.APPROVED_MERGE_SHA) {
              core.setFailed(
                `PR merge revision changed while approval was pending: ` +
                `expected ${process.env.APPROVED_MERGE_SHA}, current ${pull.merge_commit_sha}`,
              );
              return;
            }

            core.setOutput('base_sha', pull.base.sha);
            core.setOutput('merge_sha', pull.merge_commit_sha);

      - name: Checkout trusted workflow code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ steps.pr.outputs.merge_sha }}
          path: workflow

      - name: Checkout approved merge revision
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ steps.pr.outputs.merge_sha }}
          path: ops

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: python -m pip install --disable-pip-version-check pyyaml

      - name: Install Argo CD CLI
        run: |
          if command -v argocd >/dev/null 2>&1; then
            argocd version --client
            exit 0
          fi

          sudo curl -sSL \
            https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64 \
            -o /usr/local/bin/argocd
          sudo chmod +x /usr/local/bin/argocd
          argocd version --client

      - name: Apply approved Applications
        env:
          GITHUB_BASE_SHA: ${{ steps.pr.outputs.base_sha }}
          GITHUB_HEAD_SHA: ${{ steps.pr.outputs.merge_sha }}
          GITHUB_PR_NUMBER: ${{ needs.request.outputs.number }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          ARGOCD_SERVER: ${{ secrets.ARGOCD_SERVER }}
          ARGOCD_AUTH_TOKEN: ${{ secrets.ARGOCD_DEPLOY_AUTH_TOKEN }}
          ARGOCD_GRPC_WEB: ${{ vars.ARGOCD_GRPC_WEB }}
          ARGOCD_INSECURE: ${{ vars.ARGOCD_INSECURE }}
          ARGOCD_PLAINTEXT: ${{ vars.ARGOCD_PLAINTEXT }}
        working-directory: ops
        run: |
          set -o pipefail
          python -u ../workflow/.github/scripts/gitops-pr-apply.py 2>&1 \
            | tee ../gitops-pr-apply.log

      - name: Upload deployment output
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: gitops-pr-apply-${{ needs.request.outputs.number }}-${{ needs.request.outputs.merge_sha }}
          path: |
            ops/gitops-pr-apply-output
            gitops-pr-apply.log
          if-no-files-found: ignore
          retention-days: 30

  report:
    needs:
      - request
      - deploy
    if: >-
      always() &&
      needs.request.result == 'success' &&
      needs.request.outputs.app_count != '0'
    runs-on: ubuntu-latest

    steps:
      - name: Create friday-pa token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.FRIDAY_PA_APP_ID }}
          private-key: ${{ secrets.FRIDAY_PA_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}

      - name: Report deployment result
        uses: actions/github-script@v7
        env:
          DEPLOY_RESULT: ${{ needs.deploy.result }}
          DEPLOY_STARTED: ${{ needs.deploy.outputs.started }}
          APP_COUNT: ${{ needs.request.outputs.app_count }}
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const marker = '<!-- friday-pa:argocd-apply -->';
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number,
              per_page: 100,
            });
            const existing = comments.find(
              (comment) => comment.body && comment.body.includes(marker),
            );
            if (!existing) {
              core.setFailed('Argo CD deployment comment was not found');
              return;
            }

            const runUrl =
              `${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}`;
            let status;
            let result;
            if (process.env.DEPLOY_RESULT === 'success') {
              status = 'Deployed successfully';
              result =
                `${process.env.APP_COUNT} Application(s) synced and passed Argo CD health checks.`;
            } else if (process.env.DEPLOY_STARTED === 'true') {
              status = 'Deployment failed';
              result = `Approval completed, but the Argo CD deployment failed. [View workflow](${runUrl})`;
            } else {
              status = 'Deployment skipped';
              result = 'The `olympus` approval was rejected or not granted; no deployment was performed.';
            }

            let body = existing.body.replace(/^\*\*Status:\*\*.*$/m, `**Status:** ${status}`);
            body = body.replace(/\n\*\*Result:\*\*.*$/m, '');
            body = `${body.trim()}\n\n**Result:** ${result}\n`;
            await github.rest.issues.updateComment({
              owner,
              repo,
              comment_id: existing.id,
              body,
            });
