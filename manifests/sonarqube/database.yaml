apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: sonarqube-database
  namespace: sonarqube
spec:
  instances: 1

  storage:
    size: 2Gi
    storageClass: longhorn

  bootstrap:
    initdb:
      database: sonarqube
      owner: sonarqube
