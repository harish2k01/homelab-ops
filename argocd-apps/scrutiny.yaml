apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: scrutiny
  namespace: argocd

spec:
  project: default

  sources:
    - repoURL: https://harish2k01.github.io/helm-charts
      chart: scrutiny
      targetRevision: 0.2.0
      helm:
        releaseName: scrutiny
        valueFiles:
          - $values/charts/scrutiny/values.yaml

    - repoURL: https://github.com/harish2k01/homelab-ops
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

  revisionHistoryLimit: 3
